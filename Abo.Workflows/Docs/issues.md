List of known bugs/issues. Move completed line to closed bugs. Add commitid.

## open bugs and issues
B001: the entries in active_projects.json do not have a status: should be pending, running or so
B002: add a view to the webinterface to have a look at the llm calls and responses, autoupdate the view, newest entry at the top

## closed bugs and issues
B003: mattermost: "typing" is not shown (anymore) the agent is working [CLOSED: c2afdbc - fix(B003): Typing-Indikator via WebSocket statt REST-Endpoint senden]
