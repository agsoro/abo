# Abo.Workflow Idea

This project was initially a standalone Web API dedicated to BPMN process definitions. It hosted the `/api/processes` endpoints and the visual BPMN frontend (`/processes/index.html`). 

It has been moved to the `Ideas` folder as an experimental or parked idea for now to reduce clutter in the main solution while preserving the technical foundation for future development.
